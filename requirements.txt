# YellowLine AI v8 — Python dependencies
# Install: pip install -r requirements.txt --break-system-packages

# Core vision
ultralytics>=8.0.0
opencv-python>=4.8.0
numpy>=1.24.0

# Web server
flask>=3.0.0
flask-socketio>=5.3.0
eventlet>=0.35.0

# Cross-platform TTS (replaces Windows-only PowerShell/SAPI)
pyttsx3>=2.90

# Cross-platform audio playback (replaces winsound)
sounddevice>=0.4.6
scipy>=1.11.0          # for scipy.io.wavfile used by sounddevice playback

# Optional: simpleaudio fallback
# simpleaudio>=1.0.4   # uncomment if sounddevice unavailable on your platform

# ML / crowd prediction
scikit-learn>=1.3.0
pandas>=2.0.0
joblib>=1.3.0

# Fuzzy logic risk engine
scikit-fuzzy>=0.4.2
networkx>=3.0           # required by scikit-fuzzy

# Database
mysql-connector-python>=8.1.0  # optional — comment out if not using MySQL

# HTTP client (Telegram alerts, multi-camera coordinator polling)
requests>=2.31.0

# Optional: Raspberry Pi / ToF sensor support
# adafruit-circuitpython-vl53l1x>=1.3.0
# adafruit-blinka>=8.0.0

# Optional: MQTT for sensor-to-dashboard bridge
# paho-mqtt>=1.6.1

# TimescaleDB / PostgreSQL (Codex storage module)
psycopg2-binary>=2.9.0

# soundfile (used by sounddevice for WAV loading)
soundfile>=0.12.0
